PORT=5000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=online_voting_system

JWT_SECRET=replace_with_a_long_random_string
JWT_EXPIRES_IN=1d

# Comma-free single origin of your deployed frontend (Vercel URL in production)
CORS_ORIGIN=http://localhost:5173

MAX_UPLOAD_MB=5
